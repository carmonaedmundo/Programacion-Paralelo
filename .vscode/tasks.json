{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with OpenMP",
            "type": "cppbuild",
            "command": "g++", // O "gcc" según tu lenguaje
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "-fopenmp" // <--- Esta es la clave
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}